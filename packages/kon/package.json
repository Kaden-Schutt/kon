{
  "name": "@schuttdev/kon",
  "version": "0.5.6",
  "type": "module",
  "license": "MIT",
  "description": "Lightweight kond client for Claude code execution",
  "bin": {
    "kon": "dist/index.js"
  },
  "files": [
    "dist/index.js",
    "README.md"
  ],
  "scripts": {
    "build": "bun build src/index.ts --outfile dist/index.js --target node && printf '#!/usr/bin/env node\\n' | cat - dist/index.js > dist/_tmp.js && mv dist/_tmp.js dist/index.js",
    "build:bin": "bun build --compile --target bun-linux-x64 src/index.ts --outfile dist/kon-linux-x64",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@gigai/shared": "*",
    "citty": "^0.1.6",
    "zod": "^3.22.0",
    "undici": "^6.0.0",
    "typescript": "^5.3.3"
  }
}
